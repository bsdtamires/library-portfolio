-- consistency_read_status_known_values.sql
--
-- Dimension: CONSISTENCY
-- Convention: this query should return ZERO rows when the data is correct.
--
-- What to check: where `read_status` is set, it should only ever be one
-- of a fixed, known set of values. Decide what that set is and whether it
-- should be case-sensitive.

SELECT 'TODO: implement this check' AS status;
